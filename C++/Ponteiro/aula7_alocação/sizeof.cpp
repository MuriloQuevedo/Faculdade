#include <stdio.h>
#include <stdlib.h>

main()
{
    int x;
    int vetor[10];

        printf("%li\n", sizeof(x)); // sizeof retorna o tamanho em bytes de x, recebe qualquer tipo de dado.
        printf("%li\n", sizeof(vetor)); 
}

